<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:weightSum="3"
    tools:ignore="MissingDefaultResource">

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2.46" />

    <me.relex.circleindicator.CircleIndicator
        android:id="@+id/indicator"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="0.3"
        app:ci_drawable="@drawable/circle_selected"
        app:ci_drawable_unselected="@drawable/circle_unselected"
        app:ci_height="5dp"
        app:ci_margin="5dp"
        app:ci_width="5dp"
        app:layout_constraintBottom_toBottomOf="@id/skipContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/skipContainer" />

    <Button
        android:id="@+id/btnGetStarted"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:visibility="gone"
        android:layout_gravity="bottom"
        android:layout_marginStart="@dimen/bottom_margin_10"
        android:layout_marginEnd="@dimen/bottom_margin_10"
        android:layout_weight="0.19"
        android:background="@drawable/bg_rectangle_filled_primary"
        android:elevation="16dp"
        android:text="next"
        android:textAllCaps="false"
        android:textColor="@color/colorWhite"
        android:translationZ="2dp" />


</LinearLayout>

